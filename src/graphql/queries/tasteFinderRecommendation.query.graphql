query tasteFinderRecommendation($profile: TasteFinderProfile!) {
  tasteFinderRecommendation(profile: $profile, algorithm: "SQUARE_DIFFERENCE") {
    recommendations {
      shopifyId
      score
      acidity
      bitterness
      sweetness
      body
    }
  }
}
