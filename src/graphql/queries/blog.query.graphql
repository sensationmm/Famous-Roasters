query blog($slug: String!, $locale: Locale!) {
  brewingMethods(where: {slug: $slug}, locales: [$locale]) {
    id
    locale
    seoTitle
    seoMetaDescription
    content {
      html
      text
    }
    slug
    createdBy {
      name
      picture
    }
  }
}
